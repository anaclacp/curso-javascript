// ! Atribuição 

var numero = 20;

// numero = numero + 10
// console.log(numero);

// ? para abreviar isso (não ter que repetir), basta:
numero += 10;
console.log(numero);

// ! Ternário

var idade = 19;
var naoPossuiDiabetes = true
var podeBeber = (idade >= 18 && naoPossuiDiabetes) ? 'Pode beber' : 'Não pode beber';
console.log(podeBeber)

// ? condição ? true : false